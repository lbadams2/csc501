/* user.c - main */

#include <conf.h>
#include <kernel.h>
#include <proc.h>
#include <stdio.h>
#include <lab0.h>

/*------------------------------------------------------------------------
 *  main  --  user main program
 *------------------------------------------------------------------------
 */

int main()
{
	kprintf("\n\nHello World, Xinu lives\n\n");
	long input = 23847908;
	kprintf("zfunction input 0x%08x\n", input);
	long res = zfunction(input);
	kprintf("zfunction res 0x%08x\n\n", res);
	printtos();
    printprocstks(0);
	printsegaddress();
	return 0;
}
